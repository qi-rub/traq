ext fn Matrix(Fin<N>, Fin<M>) -> Bool end

fn IsEntryZero(i: Fin<N>, j: Fin<M>) -> Bool do
	e <- Matrix(i, j);
	e <- !e;
	return e
end

fn IsRowAllOnes(i: Fin<N>) -> Bool do
	hasZero <- @any<Fin<M>>[IsEntryZero(i, _)];
	ok <- not hasZero;
	return ok
end

fn HasAllOnesRow() -> Bool do
	ok <- @any<Fin<N>>[IsRowAllOnes(_)];
	return ok
end
